rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Require valid authentication for all read/write operations
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

